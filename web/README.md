# mdReview browser application

This directory contains the TypeScript and Preact browser application.

## TypeScript

The project pins one TypeScript compiler. The `typescript` package supplies both the `tsc6`
executable used by `npm run typecheck` and the compiler API used by `typescript-eslint`.
