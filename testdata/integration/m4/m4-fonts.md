# Offline typography

PT Serif renders the reading surface with _real italic text_ and **strong headings**.

The interface uses Inter, while inline code such as `const durable = true` uses JetBrains Mono.

```go
func review() string {
	return "bundled and offline"
}
```
